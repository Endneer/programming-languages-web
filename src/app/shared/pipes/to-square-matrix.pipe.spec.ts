import { ToSquareMatrixPipe } from './to-square-matrix.pipe';

describe('ToSquareMatrixPipe', () => {
  it('create an instance', () => {
    const pipe = new ToSquareMatrixPipe();
    expect(pipe).toBeTruthy();
  });
});
